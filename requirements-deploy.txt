# =============================================================
# Dr Prompt – DEPLOYMENT requirements (Render / cloud)
# =============================================================
# This is a TRIMMED version of requirements.txt.
# Heavy local packages (CUDA, torch, vLLM, Ray) are excluded
# to keep the Docker image within Render free-tier limits.
# =============================================================

# --- Web Framework ---
fastapi
uvicorn[standard]
python-multipart
starlette
gunicorn<24

# --- HTTP Clients ---
httpx
requests
aiohttp

# --- Data Validation ---
pydantic
pydantic-settings

# --- MLflow (latest stable) ---
mlflow==2.20.2

# --- Prometheus ---
prometheus-client==0.24.1
prometheus-fastapi-instrumentator==7.1.0

# --- DeepEval (evaluation metrics) ---
deepeval==3.9.1

# --- Feast (feature store) ---
feast==0.61.0

# --- OpenAI / Groq compatible client ---
openai==2.24.0
litellm==1.82.4

# --- Guardrails ---
guardrails-ai==0.9.2

# --- Utilities ---
python-dotenv==1.2.2
anyio==4.12.1
tenacity>=8.0.0,<9.0.0
rich
loguru
PyYAML
click
packaging<25

# --- Data ---
numpy==2.2.6
pandas==2.3.3
pyarrow==23.0.1

# --- Testing ---
pytest==9.0.2
pytest-asyncio==1.3.0
alembic==1.15.1
sqlalchemy==2.0.38
